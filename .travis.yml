language: minimal
sudo: required

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - lsb_release -a
  - make -v
  - docker -v

script:
  - pwd
  - make
  - ls -l dl/
  - ls -l out/
